{
  "name": "vtag-interview",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:24",
  "postCreateCommand": "npm install && npm run seed && npm install -g @anthropic-ai/claude-code",
  "secrets": {
    "ANTHROPIC_API_KEY": {
      "description": "Anthropic API key — gives you access to Claude Code (claude command in terminal)"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "alexcvzz.vscode-sqlite"
      ],
      "settings": {
        "editor.formatOnSave": true
      }
    }
  }
}
